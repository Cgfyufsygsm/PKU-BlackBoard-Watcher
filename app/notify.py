from __future__ import annotations

def send_bark(endpoint: str, title: str, body: str, url: str | None = None) -> None:
    raise NotImplementedError("Bark push is not part of the minimal login-only MVP.")

